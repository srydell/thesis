#include "Graph.h"
#include <cmath>
#include <iostream>
#include <vector>

int main(){
	// Length of one side of the lattice
	int length = 800;
	// 2D, 3D, ...
	int dimension = 2;
	Graph g(dimension, length);
}
